version: '3.8'

services:
  mongodb:
    image: mongo:latest
    container_name: lifelink-mongodb
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db

  redis:
    image: redis:latest
    container_name: lifelink-redis
    ports:
      - "6379:6379"

volumes:
  mongo-data:
